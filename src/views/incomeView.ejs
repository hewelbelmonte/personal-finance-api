<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Receitas</title>
    <link rel="stylesheet" href="/css/pages.css">
    <link rel="icon" href="/pics/save-money.png" type="image/png">
</head>
<body>
    <h1>Receitas</h1>

    <form action="<%= renda ? '/rendas/edit/' + renda.id : '/rendas/new' %>" method="POST">
        <h2><%= renda ? 'Editar Renda' : 'Adicionar Nova Renda' %></h2>
        <label for="valor">Valor:</label>
        <input type="number" id="valor" name="valor" step="0.01" value="<%= renda ? renda.valor : '' %>" required>
        <label for="descricao">Descrição:</label>
        <input type="text" id="descricao" name="descricao" value="<%= renda ? renda.descricao : '' %>" required>
        <button type="submit"><%= renda ? 'Atualizar' : 'Adicionar' %></button>
    </form>

    <h2>Lista de Receitas</h2>
    <ul>
        <% rendas.forEach(renda => { %>
            <li>
                <%= renda.descricao %> - R$ <%= renda.valor.toFixed(2) %>
                <a href="/rendas/edit/<%= renda.id %>">Editar</a>
                <form action="/rendas/delete/<%= renda.id %>" method="POST" style="display:inline;">
                    <button type="submit">Excluir</button>
                </form>
            </li>
        <% }); %>
    </ul>

    <a href="/" class="back-button">Voltar para o início</a>
</body>
</html>
